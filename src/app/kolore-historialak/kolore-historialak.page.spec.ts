import { ComponentFixture, TestBed } from '@angular/core/testing';
import { KoloreHistorialakPage } from './kolore-historialak.page';

describe('KoloreHistorialakPage', () => {
  let component: KoloreHistorialakPage;
  let fixture: ComponentFixture<KoloreHistorialakPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(KoloreHistorialakPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
